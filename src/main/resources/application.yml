#RabbitMQ Configuration
spring:
  cloud:
    function:
      definition: EventConsumer
    stream:
      bindings:
        eventConsumer-in-0:
          destination: test.sweng
          binder: cloud_rabbit
      binders:
        cloud_rabbit:
          type: rabbit
          environment:
            spring:
              rabbitmq:
                host: rabbitmq.team12.svc.cluster.local
                port: 5672
                username: admin
                password: password
                virtual-host: test
#repeat the rabbitmq credentials as follows when actuator health checks are active:
  rabbitmq:
    host: rabbitmq.team12.svc.cluster.local
    port: 5672
    username: admin
    password: password
    virtual-host: test